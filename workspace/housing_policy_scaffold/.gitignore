# Outputs and logs
40_outputs/
60_logs/

# Data and sources (keep large files out of git)
50_data/
10_sources/

# Local secrets (template for overrides; do not commit real keys)
00_admin/settings.local.yaml

# macOS
.DS_Store

# Xcode / Derived data
DerivedData/
*.xcuserdatad/
xcuserdata/
build/
