# .env.template - Environment Variable Template for Preflight Checks
#
# Copy this file to .env and fill in your actual values.
# DO NOT commit the .env file to version control!
#
# Usage:
#   cp .env.template .env
#   # Edit .env with your actual credentials
#

# GitHub Personal Access Token (for API checks)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos), public_repo (for public repos)
GITHUB_TOKEN=

# GitHub API Base URL (usually no need to change)
GITHUB_API_URL=https://api.github.com

# Optional: Custom report output directory
# If not set, reports will be saved to capstone/reports/
PREFLIGHT_REPORT_DIR=

# Optional: Timeout for external API calls (seconds)
PREFLIGHT_API_TIMEOUT=15

# Optional: Enable verbose logging
PREFLIGHT_VERBOSE=false

# Optional: Skip specific checks (comma-separated)
# Available: python_environment,git_config,github_api,disk_space,macos_specific,smart_health,icloud_sync
PREFLIGHT_SKIP_CHECKS=

# =============================================================================
# Security Notes:
# - Never commit this file with actual credentials
# - Use environment-specific .env files (.env.local, .env.production, etc.)
# - Rotate tokens regularly
# - Use least-privilege access for API tokens
# =============================================================================
